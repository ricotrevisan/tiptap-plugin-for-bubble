import { elementClicked, hideElement } from "@buildprint/bubblescript";

export default elementClicked("Cancel math button", { id: "bpmthwfx", actions: [hideElement({ id: "bpmthwx1", element: "Math popup" })] });
