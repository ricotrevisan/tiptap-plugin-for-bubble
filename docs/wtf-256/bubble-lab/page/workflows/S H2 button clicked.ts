import { pluginAction, element, elementClicked, pluginActionRef, setCustomState } from "@buildprint/bubblescript";

export default elementClicked("S H2 button", {
	id: "bwuwvffm",
	actions: [pluginAction(pluginActionRef("🤌 Rich text editor (Tiptap.dev)", "H2"), { id: "bwpysspp", element: "Scroll editor" }), setCustomState({ id: "bwzqhgzy", element: "lifecycle-lab", state: "s h2", value: element("lifecycle-lab").state("s h2").plus(1) })],
});
