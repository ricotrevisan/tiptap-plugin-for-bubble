import { pluginAction, element, elementClicked, pluginActionRef, setCustomState } from "@buildprint/bubblescript";

export default elementClicked("F H1 button", {
	id: "bwuprblg",
	actions: [pluginAction(pluginActionRef("🤌 Rich text editor (Tiptap.dev)", "H1"), { id: "bwijfnip", element: "Floating editor" }), setCustomState({ id: "bwueqxrk", element: "lifecycle-lab", state: "f h1", value: element("lifecycle-lab").state("f h1").plus(1) })],
});
