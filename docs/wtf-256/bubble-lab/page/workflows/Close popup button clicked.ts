import { elementClicked, hideElement } from "@buildprint/bubblescript";

export default elementClicked("Close popup button", {
	id: "bwbhnnue",
	actions: [hideElement({ id: "bwnnhbjr", element: "Lab popup" })],
});
