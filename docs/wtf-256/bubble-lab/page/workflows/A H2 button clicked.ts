import { pluginAction, element, elementClicked, pluginActionRef, setCustomState } from "@buildprint/bubblescript";

export default elementClicked("A H2 button", {
	id: "bwbavoea",
	actions: [pluginAction(pluginActionRef("🤌 Rich text editor (Tiptap.dev)", "H2"), { id: "bwgxoheq", element: "Editor A" }), setCustomState({ id: "bwdhlsmb", element: "lifecycle-lab", state: "a h2", value: element("lifecycle-lab").state("a h2").plus(1) })],
});
