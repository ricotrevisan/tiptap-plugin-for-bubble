import { pluginAction, element, elementClicked, pluginActionRef, setCustomState } from "@buildprint/bubblescript";

export default elementClicked("P H1 button", {
	id: "bwxzuulm",
	actions: [pluginAction(pluginActionRef("🤌 Rich text editor (Tiptap.dev)", "H1"), { id: "bwbdqjyk", element: "Popup editor" }), setCustomState({ id: "bwatzidx", element: "lifecycle-lab", state: "p h1", value: element("lifecycle-lab").state("p h1").plus(1) })],
});
