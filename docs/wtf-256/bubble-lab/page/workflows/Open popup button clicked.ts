import { elementClicked, showElement } from "@buildprint/bubblescript";

export default elementClicked("Open popup button", {
	id: "bwmnnwtt",
	actions: [showElement({ id: "bwaokfxb", element: "Lab popup" })],
});
