# Issue tracker: Linear

Issues and specs for this repo live in the Tiptap project in the ricowtf Linear workspace.

- Project: https://linear.app/ricowtf/project/tiptap-1454e1d557c0
- Team: Ricowtf (`WTF`)
- Project ID: `1aca6724-a0c1-4f95-a37f-9fd367e37736`
- Team ID: `21f3cb71-7aad-44f0-9657-cf82f5ca9710`

Use the Linear connection in the **personal** Loggie account. Before any Loggie call, read `~/.loggie/ACCOUNTS.md`; prefix every call with `~/.local/bin/loggie-account personal`. Discover the required Linear operation before calling it, and verify that the workspace URL key is `ricowtf` before writes.

When a skill says “publish to the issue tracker,” create an issue in this project and team. When it says “fetch the relevant ticket,” read the referenced `WTF-*` issue, including its description, labels, status, and comments. Keep follow-up discussion and triage changes on that issue. Use the label names in `triage-labels.md`.

For wayfinding, keep the map and its child tickets in this project. Link children and blockers with Linear issue relationships where available; record the map issue ID on each child so the set remains discoverable. Claim an unassigned, unblocked child before work; add the answer and resolve it when complete.
