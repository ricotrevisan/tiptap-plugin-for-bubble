import { elementClicked, showElement } from "@buildprint/bubblescript";

export default elementClicked("Show editor", { id: "bpjlgwer", actions: [showElement({ id: "bpvsynnu", element: "Bound record" })] });
