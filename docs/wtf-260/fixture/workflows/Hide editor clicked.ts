import { elementClicked, hideElement } from "@buildprint/bubblescript";

export default elementClicked("Hide editor", { id: "bpcvjihu", actions: [hideElement({ id: "bpufsrpy", element: "Bound record" })] });
