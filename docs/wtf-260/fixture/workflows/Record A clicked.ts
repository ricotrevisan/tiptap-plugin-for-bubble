import { elementClicked, setCustomState } from "@buildprint/bubblescript";

export default elementClicked("Record A", {
	id: "bpgmfrvh",
	actions: [setCustomState({ id: "bpskloyh", element: "wtf-260-autobinding", state: "Selected record", value: "A" })],
});
