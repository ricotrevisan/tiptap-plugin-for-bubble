import { elementClicked, setCustomState } from "@buildprint/bubblescript";

export default elementClicked("Delay 300", {
	id: "bpknljot",
	actions: [setCustomState({ id: "bplneoww", element: "wtf-260-autobinding", state: "Save delay", value: 300 })],
});
